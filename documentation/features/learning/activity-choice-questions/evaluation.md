---
title: Learning Activity choice questions evaluation
status: completed
spec: ./spec.md
spec_revision: 3
plan: ./plan.md
source: https://joaogoliveiragarcia.atlassian.net/browse/SHIFU-74
prd:
  learning: { content_id: 83066881, version: 13 }
  curriculum: { content_id: 83034113, version: 6 }
last_updated_at: 2026-09-23
---

# Evaluation status

- **Status:** `completed`; ACH-01–ACH-09 are resolved, the single Implementation Reviewer
  confirmed the corrections, and required evidence is current for Spec revision 3.
- **Contract:** Spec revision 3, `ready` after Spec Reviewer compatibility pass;
  Learning PRD 83066881 v13 and Curriculum
  PRD 83034113 v6 are current and match recorded traceability. Jira SHIFU-74 remains
  `A fazer`; no external Jira or Confluence state was changed.
- **Plan:** `completed`, all F1/F2 tasks and F3 are complete. ACH-06 Web mapping,
  ACH-07 Server result hydration, ACH-08 compact display, and ACH-09 navigation wrapper
  corrections are verified; EV-24 confirms three persisted result details render live.
- **Baseline:** existing worktree has a pre-existing `M design/shifu.pen` and the
  untracked feature artifact directory containing Spec, Plan and nine saved design
  references. Those user artifacts are preserved. The Activity route is the current
  not-found placeholder; no choice-attempt tests, Learning job, Activity REST-client
  file or dedicated nested Activity route suites exist.
- **Runtime baseline:** Compose `postgres`, `inngest`, `redis`, and supporting local
  services are running. FastAPI `localhost:7777/health` and web `localhost:7000` are
  unavailable (connection refused). Learning job registration and a disposable
  published browser fixture are not yet verified. Browser/runtime and migration
  scenarios remain pending until their required application and disposable fixtures
  are available.
- **Baseline checks:** no feature tests or quality gates have been run before edits;
  the first CI runs will be recorded against the current implementation candidate.
- **Next action:** local SDD conclusion is complete. Commit and PR publication were not
  performed; they require the separately authorized publication handoff.

## Builder assignments

| Builder identifier | Plan assignment | Exact ownership | Allowed paths | Prohibited paths | Status |
| --- | --- | --- | --- | --- | --- |
| `/root/learning_activity_server_builder` | F2-T1 — Builder Server | Learning controllers/router, pipe/app composition, Inngest job/registration, error mapping, REST examples/tests; fixed-contract answer normalization and replay outcome | Exact F2-T1 paths enumerated in `plan.md`, including named F1 correction paths | Web, other F1-owned paths, and all SDD/design/authority files | `complete; Spec rev3` |
| `/root/learning_activity_web_builder` | F2-T2 — Builder Web | REST adapter/service, nested Activity/attempt routes, five route/Page suites; saved-attempt recovery, result polling and navigation-warning hook corrections | Exact F2-T2 paths enumerated in `plan.md`, including the two Page hooks and their hook suites | Server, generated route metadata, other F1-owned paths and all SDD/design/authority files | `complete; Spec rev3` |
| `/root/learning_activity_web_builder` | F3 ACH-06 — Builder Web Fix | Map attempt result fields from the actual Server response | `apps/web/src/rest/services/learning-service.ts` only | Every other path, Server, generated routes, SDD/design/authority and external systems | `complete; typecheck passed; EV-18` |
| `/root/learning_activity_server_builder` | F3 ACH-07 — Builder Server Fix | Verify completed GET attempt HTTP response and correct Server serialization/use-case/mapping if needed | `apps/server/tests/learning/server/controllers/test_get_choice_attempt_controller.py`; conditionally `apps/server/src/shifu/learning/rest/controllers/get_choice_attempt_controller.py`, `apps/server/src/shifu/learning/core/use_cases/get_choice_attempt_use_case.py`, `apps/server/src/shifu/learning/database/sqlalchemy/mappers/activity_attempt_mapper.py`, `apps/server/src/shifu/learning/database/sqlalchemy/mappers/activity_evaluation_mapper.py` | Every other path, all Web, generated routes, SDD/design/authority and external systems | `complete; Spec rev3; verified EV-21 and current Server gates` |
| `/root/learning_activity_web_builder` | F3 ACH-08 — Builder Web Fix | Normalize display-only score formatting against saved result references while preserving exact API/domain values | `apps/web/src/ui/learning/widgets/pages/choice-result-page/index.tsx`, `apps/web/src/ui/learning/widgets/pages/choice-result-page/choice-result-detail/index.tsx`, and their colocated test files only | Server, REST/domain scoring, all other Web paths, generated routes, SDD/design/authority and external systems | `complete; Spec rev3; focused exits EV-22` |
| `/root/learning_activity_web_builder` | F3 ACH-09 — Builder Web Fix | Route Activity/attempt navigation through the shared navigation wrapper, including login redirect | `apps/web/src/ui/shared/hooks/use-navigation.ts`; the Activity and attempt `index.tsx` route wrappers; existing route suites only if needed | Server, unrelated Web paths, generated routes, SDD/design/authority and external systems | `complete; Spec rev3; integrated gates EV-32` |

Assignments use the fixed RF/CA mappings and selected RP/JN mappings in each Plan
task card. Each Builder must read its complete Rule Pack and the Design Contract
before source edits. Builders report evidence; the Orchestrator verifies it and owns
all SDD artifacts, generated route metadata, integration and final validation.
The Activity question `kind` wire discriminator is `single_choice` or
`multiple_selection`; the Spec defines the field but leaves its literal enum values
unnamed. Both server and Web adapters must use these same values.

## Acceptance coverage

| Criterion | Spec coverage | Required evidence | Disposition | Status |
| --- | --- | --- | --- | --- |
| CA-01 | RF-01, RF-11 | Server use-case/controller tests; route suites; VM-01, VM-05 | Owner Activity flow persists and returns; anonymous receives 401; private absence remains safe 404. | `passed` — EV-24, EV-26, EV-29, EV-32 |
| CA-02 | RF-02, RF-10 | Widget/hook/route suites; VM-01, VM-02 | Ordered radio/checkbox interactions, sequential flow and no-draft leave guard pass; keyboard radio behavior is covered by ChoiceQuestion suite. | `passed` — EV-26, EV-29, EV-32 |
| CA-03 | RF-03, RF-09 | Use-case/controller/PostgreSQL tests; VM-01, VM-06, VM-07 | Immutable attempt, submission-key replay, outbox and migration preservation verified on disposable databases. | `passed` — EV-13, EV-26, EV-29 |
| CA-04 | RF-04, RF-11 | Use-case and real Inngest job tests; VM-03 | Exact question score and curriculum-weighted final score pass use-case/job boundaries; displayed decimals stay compact. | `passed` — EV-22, EV-29, EV-30 |
| CA-05 | RF-05, RF-09 | Controller/job/widget/route tests; VM-03, VM-06, VM-07 | Pending/failure/retry/stale recovery and durable attempt return pass with no score on pending/failure. | `passed` — EV-26, EV-29, EV-32 |
| CA-06 | RF-06, RF-10 | Controller/widget/route tests; VM-03, VM-04 | Three persisted question details render at desktop/mobile; five-question mobile layout scrolls without horizontal overflow or final-card/navigation overlap. | `passed` — EV-24, EV-28, EV-29, EV-32 |
| CA-07 | RF-07 | Use-case/controller security tests; VM-03 | Incorrect result response and rendered state omit undisclosed correct options. | `passed` — EV-24, EV-26, EV-29 |
| CA-08 | RF-07, RF-08 | Use-case/controller tests; VM-03, VM-05 | Correct selections release their answer keys and fixed outcome explanation; incorrect paths retain safe disclosure. | `passed` — EV-24, EV-26, EV-29 |
| CA-09 | RF-08, RF-09 | PostgreSQL/controller/job tests; VM-06, VM-07 | Duplicate/stale delivery and deletion produce one effect/event and no Skill recreation; schema lifecycle passes. | `passed` — EV-13, EV-29 |
| CA-10 | RF-02, RF-05, RF-06, RF-10 | Widget/route suites; VM-01–VM-04 | Keyboard radio path, responsive states, mobile leave warning and scrollable results are covered at saved viewports. | `passed` — EV-24, EV-26, EV-28, EV-29, EV-32 |
| CA-11 | RF-01, RF-11 | Provider/use-case/controller tests; VM-05 | Legacy incomplete explanation snapshots remain unavailable; eligible authored snapshots expose only safe question data. | `passed` — EV-26, EV-29 |
| CA-12 | RF-03, RF-06, RF-09 | Controller/job/route suites; VM-03, VM-06 | Attempt result remains available at the same route and polling/retry returns to the same attempt. | `passed` — EV-24, EV-26, EV-29, EV-32 |

## Automated gates

| CI ID | Command/sensor | Coverage | Result | Evidence |
| --- | --- | --- | --- | --- |
| CI-01 | `pnpm --filter web check:lint` (repository root) | Web lint | `passed` | EV-30, EV-32 |
| CI-02 | `pnpm --filter web check:architecture` (repository root) | Web import boundaries | `passed` | EV-30, EV-32 |
| CI-03 | `pnpm --filter web check:types` (repository root) | Web type contracts | `passed` | EV-30, EV-32 |
| CI-04 | `pnpm --filter web test:unit` (repository root) | Web widget and hook suites | `passed` | EV-30, EV-32 |
| CI-05 | `pnpm --filter web test:integration` (repository root; isolated PostgreSQL/Redis/Inngest) | Routed browser suites | `passed` | EV-26, EV-32 |
| CI-06 | `pnpm --filter web build` (repository root) | Generated route metadata and Web build | `passed` | EV-30, EV-32 |
| CI-07 | `uv run poe check:lint` (`apps/server`) | Python lint | `passed` | EV-29 |
| CI-08 | `uv run poe check:architecture` (`apps/server`) | Server module boundaries | `passed` | EV-29 |
| CI-09 | `uv run poe check:types` (`apps/server`) | Server typing | `passed` | EV-29 |
| CI-10 | `uv run poe test:unit` (`apps/server`) | Domain and use cases | `passed` | EV-29 |
| CI-11 | `uv run poe test:integration` (`apps/server`) | Real HTTP/PostgreSQL | `passed` | EV-29 |
| CI-12 | `uv run poe test:jobs` (`apps/server`) | Disposable Inngest job | `passed` | EV-29 |
| CI-13 | `uv run poe build` (`apps/server`) | Server build | `passed` | EV-29 |
| CI-14 | `uv run poe db:upgrade f64a8c3d7e21` (`apps/server`; disposable DB only) | Migration preflight and upgrade | `passed` | EV-13 |
| CI-15 | `uv run poe db:downgrade e1a2b3c4d5e6` then `uv run poe db:upgrade f64a8c3d7e21` (`apps/server`; disposable DB only) | Migration downgrade/re-upgrade | `passed` | EV-13 |

## Manual and visual evidence

| VM / state | Expected behavior | Observed behavior | Exact viewport/state | Reference | Artifact path | Status |
| --- | --- | --- | --- | --- | --- | --- |
| VM-01 desktop owner flow | Safe ordered read, sequential answers, one idempotent attempt | Authenticated owner read ordered three questions, submitted once, persisted one attempt/evaluation and completed result with all three details; no failed response or console error. | 1440 × 900; owner question flow and submit | `o2q7H.png`, `Cj8R7.png` | `/tmp/shifu-activity-choice-evidence/VM-01-live-desktop-question.png`; EV-24 | `passed` |
| VM-02 mobile selection and leave warning | 44 px targets, keyboard/touch selection, no draft, no overflow | Touch selection and unsent leave warning kept the owner on Activity; no draft was persisted. Native radio keyboard selection passes; radio/checkbox labels use `min-h-11` (44 px), and mobile capture shows no horizontal overflow. | 390 × 844; multi-select and unsent leave warning | `rf857.png` | `/tmp/shifu-activity-choice-evidence/VM-02-live-mobile-unsent.png`, `/tmp/shifu-activity-choice-evidence/VIS-08-mobile-multiple.png`; EV-24, EV-29 | `passed` |
| VM-03 desktop result and disclosure | Pending has no score; complete weighted result; safe feedback/progress | Pending UI showed status with no score; persisted completed attempt rendered three details, score/progress/action; incorrect fixtures hid correct options and correct fixtures released them with fixed feedback. | 1440 × 900; pending, complete, protected and released details | `PCnZO.png`, `YDgNz.png`, `gftrs.png`, `ad7p6.png`, `ntBNV.png` | `/tmp/shifu-activity-choice-evidence/VM-03-live-desktop-completed.png`, `/tmp/shifu-activity-choice-evidence/VIS-03-desktop-single-incorrect.png`–`VIS-07-desktop-complete.png`, `VIS-10-desktop-pending.png`; EV-24, EV-26 | `passed` |
| VM-04 mobile complete result | All question details reachable by scrolling without clipping/overflow | Three-question result was persisted and viewed on mobile; authenticated five-question result rendered all five details at 390 × 844, had no horizontal overflow, and final card scrolled fully above fixed bottom navigation. | 390 × 844; three- and five-question result | `k3bUS8.png` | `/tmp/shifu-activity-choice-evidence/VM-04-live-mobile-completed.png`, `/tmp/shifu-activity-choice-evidence/VM-04-live-five-question-mobile.png`; EV-24, EV-28 | `passed` |
| VM-05 privacy and legacy content | Anonymous/private absence safe; incomplete explanation blocks eligibility | Server boundaries returned 401 for anonymous, safe 404 for private/mismatched hierarchy; use cases reject unreleased and incomplete explanation snapshots and allow only safe ordered response data. | 1440 × 900; anonymous, other account, wrong hierarchy, legacy/authored content | Activity question references | Server controller/use-case suites in EV-26, EV-29 | `passed` |
| VM-06 failure, retry and stale delivery | Same attempt/new run; one effect; deletion not recreated | Disposable Inngest job exercised duplicate and stale deliveries, failure handling, once-only effect/outbox and deleted Skill; retry uses the same attempt/new run. Pending/failure/recovered browser states and retry route assertions passed. | 1440 × 900; pending, failure, recovery | Handoff adjacent states | `/tmp/shifu-activity-choice-evidence/VIS-10-desktop-pending.png`–`VIS-12-desktop-recovered.png`; EV-26, EV-29 | `passed` |
| VM-07 migration preservation | Upgrade/downgrade/re-upgrade preserves legacy rows and index semantics | Testcontainers PostgreSQL preserved completed/pending legacy attempts and answers; pending was archived with the expected code; downgrade/re-upgrade restored the schema/index | Disposable PostgreSQL; migration states | None (database scenario) | EV-13 | `passed` |
| VIS-01 desktop single-choice question | Match approved layout, hierarchy, focus and tokens | Captured and inspected: centered 3-question Activity, serif prompt, native single-choice controls, warning and red primary action align with reference hierarchy/tokens. | 1440 × 900; question 2 of 3 | `design/o2q7H.png` | `/tmp/shifu-activity-choice-evidence/VIS-01-desktop-single.png`; EV-24 | `passed` |
| VIS-02 desktop multiple-selection question | Match approved layout and selected controls | Captured and inspected: selected/unselected checkbox rows and progression controls match approved desktop composition. | 1440 × 900; question 1 of 3 | `design/Cj8R7.png` | `/tmp/shifu-activity-choice-evidence/VIS-02-desktop-multiple.png`; EV-24 | `passed` |
| VIS-03 desktop incorrect single result | Protected answer remains undisclosed | Captured and inspected: submitted option and safe explanation render; correct option label is absent from card and DOM. | 1440 × 900; incorrect result | `design/YDgNz.png` | `/tmp/shifu-activity-choice-evidence/VIS-03-desktop-single-incorrect.png`; EV-24 | `passed` |
| VIS-04 desktop correct single result | Correct answer and fixed explanation released | Captured and inspected: selected response, released correct option and fixed explanation render in green outcome state. | 1440 × 900; correct result | `design/gftrs.png` | `/tmp/shifu-activity-choice-evidence/VIS-04-desktop-single-correct.png`; EV-24 | `passed` |
| VIS-05 desktop incorrect multiple result | Missing correct selection stays hidden | Captured and inspected: submitted rows and safe conceptual feedback render without unselected correct option labels. | 1440 × 900; incorrect multi result | `design/ad7p6.png` | `/tmp/shifu-activity-choice-evidence/VIS-05-desktop-multiple-incorrect.png`; EV-24 | `passed` |
| VIS-06 desktop correct multiple result | Correct set and fixed explanation released | Captured and inspected: exact selected correct set and fixed explanation render in the correct state. | 1440 × 900; correct multi result | `design/ntBNV.png` | `/tmp/shifu-activity-choice-evidence/VIS-06-desktop-multiple-correct.png`; EV-24 | `passed` |
| VIS-07 complete desktop result | All three-to-five details and progress fit/scroll coherently | Captured and inspected: complete three-detail result, weighted score, progress, per-question feedback and next action fit the desktop scroll. | 1440 × 900; completed result | `design/PCnZO.png` | `/tmp/shifu-activity-choice-evidence/VIS-07-desktop-complete.png`; EV-24 | `passed` |
| VIS-08 mobile multi-selection question | Touch targets, shell, focus and scrolling match approved hierarchy | Captured and inspected at 390 × 844; multi-select rows wrap without horizontal overflow; minimum target height is 44 px by shared control classes. | 390 × 844; multi-select | `design/rf857.png` | `/tmp/shifu-activity-choice-evidence/VIS-08-mobile-multiple.png`; EV-24 | `passed` |
| VIS-09 complete mobile result | Full result remains readable through vertical scroll | Captured and inspected: three-question detail cards, score, progress and next action remain in one vertical flow; full-page capture confirms all detail rows. | 390 × 844; completed result | `design/k3bUS8.png` | `/tmp/shifu-activity-choice-evidence/VIS-09-mobile-complete.png`; EV-24 | `passed` |
| VIS-10 desktop pending | Status announced; no score shown | Captured and inspected: pending heading/status text and message appear without any score or official result card. | 1440 × 900; pending evaluation | Handoff pending state | `/tmp/shifu-activity-choice-evidence/VIS-10-desktop-pending.png`; EV-24 | `passed` |
| VIS-11 desktop failure | Safe failure and focused retry action | Captured and inspected: generic safe failure, saved-answer reassurance and retry action are visible. | 1440 × 900; failed evaluation | Handoff failure state | `/tmp/shifu-activity-choice-evidence/VIS-11-desktop-failed.png`; EV-24 | `passed` |
| VIS-12 desktop recovery | Same attempt resumes after retry with new run | Captured and inspected: recovered completed attempt returns to the full result state at its original URL. | 1440 × 900; recovered evaluation | Handoff recovery state | `/tmp/shifu-activity-choice-evidence/VIS-12-desktop-recovered.png`; EV-24 | `passed` |

REST parity: `apps/server/rest-client/learning/activities.rest` contains four labeled,
route-complete requests with reusable non-secret variables; verified against the four
Learning route declarations. See EV-14. The example access token is an explicit local
placeholder, not a credential.

## Review findings

| ACH ID | Classification | Source | Affected evidence | Status | Resolution |
| --- | --- | --- | --- | --- | --- |
| ACH-01 | Blocking Rule/test-contract mismatch | `web-app-routing-rules.md` requires one suite per route file; `widget-testing-rules.md` requires one module-owned suite per routed Page | F2-T2 paths, browser coverage and conformance record | `resolved` | Spec revision 3 adds `ChoiceActivityPage` and `ChoiceResultPage` suites alongside all three per-route suites. Read-only Spec Reviewer `/root/learning_activity_spec_reviewer` returned Pass; Orchestrator verified the paths satisfy both Rules. |
| ACH-02 | In-contract F1 Web validation failures | Builder Web F1-T2 focused component/hook run and static checks | EV-09, EV-11 and F1-T2 exit | `resolved` | The initial assertions, unused symbols, formatting and radio-group semantics were corrected within assigned paths. Orchestrator reran all six focused suites (18/18), `check:types`, and scoped Biome after the retry-guard change; all passed. |
| ACH-03 | Attempt integrity / retry guard | RF-02/RF-03 and Web F1 hook | Activity hook, its test and F2 Activity route blocker | `resolved` | The Activity hook freezes the submitted answers and key for same-payload transport retry; the route wires the in-app warning and `beforeunload`. Focused Page/route suites passed the retry and unsent-navigation scenarios (EV-14). |
| ACH-04 | Fixed-Spec handoff gap | Spec §3 technical/data flow and widget path contract; F1 implementation inspection | Submit request kind normalization/replay status/typed persistence; disposable Inngest database; Activity saved-attempt routing; result polling; F2 controllers/routes, fixture, mapper and Page suites | `resolved` | Server normalizes the fixed answer body from the frozen Curriculum snapshot, preserves typed replay mapping, exposes replay status only to the controller, and migrates/exposes disposable Inngest PostgreSQL. Web routes recover unresolved attempts, poll pending results while visible, and wire retry/navigation behavior. Real controller, job, Page and route evidence is recorded in EV-14 and the Server gate evidence. No Spec or product behavior changed. |
| ACH-05 | Inngest callback rate-limit conflict | Registered real Inngest test; shared `RateLimitMiddleware` | `/api/inngest` callback and multi-step job execution | `resolved` | The middleware excludes only the exact `/api/inngest` callback path; learner-facing rate-limit buckets remain intact. The registered job completed duplicate, stale, failure and deletion scenarios on disposable PostgreSQL/Inngest Testcontainers after the fixture switched to nonblocking raw-pipe output draining (EV-14). |
| ACH-06 | Server/Web result wire-field mismatch | Live authenticated persisted flow; Server `QuestionResultResponse` and Web `AttemptWire` | CA-06/CA-07, VM-03/VM-04, VIS-03–VIS-09; EV-17 is stale for result-detail claims | `resolved` | Builder Web corrected `AttemptWire` and `mapAttempt` to consume `question_key`, `selected_option_keys`, and `disclosed_correct_option_keys`; `pnpm --filter web check:types` passed. Fresh full-stack evidence exposed the separate ACH-07 below. |
| ACH-07 | Completed result detail absent in live Web flow | Isolated full-stack authenticated browser submission and post-job persistence | CA-06–CA-08, VM-03/VM-04, VIS-03–VIS-09; EV-19, EV-21 | `resolved` | Server mapper now deserializes the persisted discriminated evaluation parts to their concrete domain result types. Controller regression confirms all question details and correct-answer disclosure rules; EV-21 confirms three persisted answers/parts render as three live detail cards. Fresh Server gates passed. |
| ACH-08 | Persisted decimal scale conflicts with compact approved score display | Fresh authenticated persisted result screenshots compared with `PCnZO`, `YDgNz`, `gftrs`, `ad7p6`, and `ntBNV` | CA-04/CA-06; VM-03/VM-04; VIS-03–VIS-09; EV-21–EV-24, EV-26, EV-28, EV-30 | `resolved` | Builder Web added pt-BR compact formatting at both result score boundaries; integer and fractional display assertions passed. Orchestrator's fresh authenticated persisted captures show compact scores; five-question mobile capture verifies full scroll; integrated Web gates/full browser suite pass. Scoring/API values remain unchanged. |
| ACH-09 | Shared navigation wrapper Rule violation | Initial Implementation Reviewer pass; `documentation/rules/ui-layer-rules.md` imperative navigation and route constants requirements | Activity and attempt route wrappers; route navigation assertions and CI-01–CI-06; existing screenshots are unaffected visually | `resolved` | Builder Web overloaded the existing shared `navigateTo` API for typed Activity/Attempt IDs, deriving paths through canonical route helpers, and migrated both wrappers including login redirects. Existing route assertions pass; Orchestrator reran all Web gates and the full isolated browser suite (EV-32). No rendered output changed. |

## Evidence log

| EV ID | Date | Scope | Exact command/scenario | Result | Finding | Runtime notes | Acceptance mapping |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EV-01 | 2026-09-23 | Pre-implementation baseline and source tree | `git status --short`; inspect current Activity route/test paths; `docker compose ps --services --filter status=running`; request `http://localhost:7777/health` and `http://localhost:7000` | Baseline recorded before feature source edits; route remains not-found placeholder; no choice attempt tests or nested route suites; API/web refused connections | No Spec conflict found. Existing modified `design/shifu.pen` and feature artifacts preserved. Runtime gate remains pending. | Compose PostgreSQL/Inngest/Redis/supporting services running; FastAPI/web and fixture unavailable | CA-01–CA-12; VM-01–VM-07 |
| EV-02 | 2026-09-23 | Authority and contract preflight | Read complete Learning PRD `83066881` v13, Curriculum PRD `83034113` v6, Jira SHIFU-74; inspect Spec revision 2, Plan, Architecture, Modules, Tooling, Rules router and design handoff | Current recorded versions and traceability confirmed; Plan F1 ownership matches distinct Server/Web paths; nine saved references mapped | No authority conflict found; no external state changed | PRDs current as of retrieval; Jira remains `A fazer` | RF-01–RF-11; CA-01–CA-12 |
| EV-03 | 2026-09-23 | Initial Spec conformance and ownership fence (historical; prior route-test conclusion invalidated by ACH-01) | Compare Spec path/widget tree, route-test parity, route-complete REST requirement and exclusions to baseline filesystem; record F1 Builder assignments | Baseline path inventory and F1 path separation remain useful; previous conclusion that route-test contract was complete is stale | ACH-01 identified the omitted Widget Rule page suites. Route metadata, app composition, Evaluation and integration remain Orchestrator-owned. | No feature source edits at this checkpoint | RF-01–RF-11; CA-01–CA-12 |
| EV-04 | 2026-09-23 | Rule-conformance amendment | Compare routed Page ownership with both selected test Rules and existing `competency-detail-page.test.ts`; add two module-owned Page suites to Spec/Plan | Spec revision 3 lists two Page suites in addition to three route-file suites; awaiting Spec Reviewer | No feature source or invalid test path edited; Builder work paused pending review | Mocked Playwright transport; real server evidence remains separate | CA-01–CA-03; CA-05–CA-08; CA-10–CA-12 |
| EV-05 | 2026-09-23 | Cross-Builder DTO wire mapping | Reconcile the Activity DTO `kind` field with API snake_case convention; inform both Builders | Shared wire literals fixed as `single_choice` and `multiple_selection` | Existing code has no choice discriminator enum; one shared mapping set for both adapters | No source edits associated | CA-01, CA-02 |
| EV-06 | 2026-09-23 | Pre-plan Spec compatibility review | Read-only review of Spec revision 3 against Architecture, Modules, migration/generated-artifact placement and complete selected Rule Pack, especially both Web test Rules | Pass; the added page suites satisfy the per-Page rule while all three per-route suites remain present | Orchestrator verified reviewer report. ACH-01 resolved; no implementation/product/design verdict was made. | Reviewer: `/root/learning_activity_spec_reviewer`; no files edited | CA-01–CA-03; CA-05–CA-08; CA-10–CA-12 |
| EV-07 | 2026-09-23 | Result selection label mapping | Coordinate Web result rendering with the fixed public Activity GET and attempt DTO | Result UI maps submitted option keys to labels only through safe Activity `{key,text}` options; hidden/missing option keys are never inferred | Existing public Activity response supplies labels; no new answer-bearing result fields added | F2 route must supply Activity DTO and attempt result to the Result Page; mapped in the Builder handoff | CA-06, CA-07, CA-08 |
| EV-08 | 2026-09-23 | Next-action wire mapping | Reconcile Attempt DTO `next_action` with existing Learning `ActivityRecommendation` contract | Reuse the existing recommendation shape: competency/activity IDs, difficulty and type (`new-activity` / `reinforcement`) | Avoid a parallel recommendation contract; Web route owns canonical destination wiring | Sent to Server and Web Builders; F2 route will pass the result recommendation to the Page | CA-06, CA-08 |
| EV-09 | 2026-09-23 | F1-T2 initial focused Web component/hook tests and static checks | `pnpm --filter web exec vitest run src/ui/learning/widgets/pages/choice-activity-page/choice-question/tests/choice-question.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/choice-activity-page.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/use-choice-activity-page.test.ts src/ui/learning/widgets/pages/choice-result-page/choice-result-detail/tests/choice-result-detail.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/choice-result-page.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/use-choice-result-page.test.ts`; `pnpm --filter web check:types`; `pnpm --filter web exec biome check src/core/learning/choice-activity.ts src/ui/shadcn/radio-group.tsx src/ui/shadcn/checkbox.tsx src/ui/learning/widgets/pages/choice-activity-page src/ui/learning/widgets/pages/choice-result-page` | Failed: focused suite 15/18 passed, three assertions failed; typecheck found unused `ActivityRecommendation` and `pageState`; Biome found formatting and `fieldset role=radiogroup` semantics. Historical evidence; corrected code requires a fresh run. | ACH-02; within-contract correction active | jsdom unload `returnValue` stayed false despite `defaultPrevented`; two tests matched duplicate visible `0` text | CA-02, CA-05–CA-08, CA-10 |
| EV-10 | 2026-09-23 | F1-T2 focused Web checks after first assertion/semantics corrections (historical; stale after ACH-03) | `pnpm --filter web exec vitest run src/ui/learning/widgets/pages/choice-activity-page/choice-question/tests/choice-question.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/choice-activity-page.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/use-choice-activity-page.test.ts src/ui/learning/widgets/pages/choice-result-page/choice-result-detail/tests/choice-result-detail.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/choice-result-page.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/use-choice-result-page.test.ts` → 6 files, 18 tests passed; `pnpm --filter web check:types` → exit 0; `pnpm --filter web exec biome check src/core/learning/choice-activity.ts src/ui/shadcn/radio-group.tsx src/ui/shadcn/checkbox.tsx src/ui/learning/widgets/pages/choice-activity-page src/ui/learning/widgets/pages/choice-result-page` → 15 files clean | Passed before the retry-guard code change; stale evidence pending rerun | ACH-03 invalidates hook/test/type/lint evidence after correction | Existing pnpm warning: `apps/web/package.json`'s `pnpm.onlyBuiltDependencies` is ineffective at app scope; no other reported warnings | CA-02, CA-05–CA-08, CA-10 |
| EV-11 | 2026-09-23 | Orchestrator F1-T2 focused Web component/hook tests and static checks after retry-guard correction | `pnpm --filter web exec vitest run src/ui/learning/widgets/pages/choice-activity-page/choice-question/tests/choice-question.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/choice-activity-page.test.tsx src/ui/learning/widgets/pages/choice-activity-page/tests/use-choice-activity-page.test.ts src/ui/learning/widgets/pages/choice-result-page/choice-result-detail/tests/choice-result-detail.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/choice-result-page.test.tsx src/ui/learning/widgets/pages/choice-result-page/tests/use-choice-result-page.test.ts` → 6 files, 18 tests passed; `pnpm --filter web check:types` → exit 0; `pnpm --filter web exec biome check src/core/learning/choice-activity.ts src/ui/shadcn/radio-group.tsx src/ui/shadcn/checkbox.tsx src/ui/learning/widgets/pages/choice-activity-page src/ui/learning/widgets/pages/choice-result-page` → 15 files clean | Passed; current evidence for F1-T2 focused tests and static checks | Existing pnpm warning: `apps/web/package.json`'s `pnpm.onlyBuiltDependencies` is ineffective at app scope; no other reported warnings | CA-02, CA-05–CA-08, CA-10 |
| EV-12 | 2026-09-23 | Orchestrator F1-T1 integrated Server gates | In `apps/server`: `uv run poe check:lint` → exit 0, all checks passed/519 files formatted; `uv run poe check:architecture` → exit 0, all modules validated; `uv run poe check:types` → exit 0, 0 errors/warnings/notes; `uv run poe test:unit` → exit 0, 41 passed, 2 existing Starlette/httpx deprecation warnings | Passed on integrated F1 candidate | Server Builder’s scoped five-suite check separately passed 15 tests; no shared database touched by unit tests | CA-01, CA-03–CA-09, CA-11–CA-12 |
| EV-13 | 2026-09-23 | Disposable PostgreSQL migration lifecycle and legacy-data assertions | Testcontainers PostgreSQL 17; `uv run poe db:upgrade e1a2b3c4d5e6`; seed one completed (score 84) and one pending legacy attempt with answer JSON; `uv run poe db:upgrade f64a8c3d7e21`; verify both rows/answers retained, completed score retained, pending archived as failed with `archived_unscorable_legacy_attempt`, and partial unique index predicate; `uv run poe db:downgrade e1a2b3c4d5e6`; verify both attempt/evaluation rows and old schema; `uv run poe db:upgrade f64a8c3d7e21`; verify rows and index restored. Separate disposable run rejected duplicate non-null scoped submission key and accepted multiple NULL legacy keys. | Upgrade, downgrade and re-upgrade passed; temporary container destroyed | No shared DB or Compose data touched | CA-03, CA-05, CA-09; VM-07; CI-14–CI-15 |
| EV-14 | 2026-09-23 | Integrated Web F2 routes, generated metadata and REST parity | `pnpm --filter web generate-routes` → exit 0; `pnpm --filter web check:lint` → exit 0 (162 files); `check:architecture` → exit 0 (156 modules, 468 dependencies); `check:types` → exit 0; `test:unit` → 29 files / 84 tests passed; `pnpm --filter web build` → exit 0; focused Playwright route/Page suites reported by Builder: `PLAYWRIGHT_HTML_OPEN=never pnpm --filter web exec playwright test --grep 'Activity route parent|Activity index route|Attempt index route|runs the actual Activity route|renders actual result route|navigates from the Competency detail' --workers=1` → 11/11 passed; compare four `.rest` requests to route declarations | Passed for route generation, static/unit/build gates, five dedicated route/Page suites and example parity. | Existing pnpm app-scope `onlyBuiltDependencies` and Node circular `replaceRouteChunk` warnings only | CA-01–CA-03, CA-05–CA-08, CA-10–CA-12; CI-01–CI-04, CI-06; REST parity |
| EV-15 | 2026-09-23 | Integrated Server F2/F3 gates | In `apps/server`: `uv run poe check:lint` → exit 0 (534 files formatted); `check:architecture` → exit 0; `check:types` → exit 0 (0 diagnostics); `test:unit` → 42 passed; `test:integration` → 32 passed on disposable PostgreSQL; `test:jobs` → 2 passed including the Learning job on disposable PostgreSQL/Redis/Inngest Testcontainers; `build` → exit 0; root `git diff --check` → exit 0; verify the four `.rest` entries against router/controller methods and paths | Passed | Only existing Starlette/httpx and deprecated status-code warnings; all temporary test containers and API processes stopped; shared Compose services/data untouched | CA-01, CA-03–CA-09, CA-11–CA-12; CI-07–CI-15; VM-06–VM-07; REST parity |
| EV-16 | 2026-09-23 | Full Web Playwright integration suite with disposable full-stack services | Initial `PLAYWRIGHT_HTML_OPEN=never pnpm --filter web test:integration -- --workers=1` → 47/50 passed, three Identity sign-in handler cases got 503 because API was absent after job fixture teardown. Reran `pnpm --filter web test:integration --workers=1` with disposable PostgreSQL 17, Redis 7, Inngest v1.41.1, API at 7777 and `BETTER_AUTH_DATABASE_URL` pointed to the same migrated database → 50/50 passed. New Learning Page/route suites all passed; auth handler cases returned expected 200/401. | Passed on isolated full-stack rerun; first environment-only attempt preserved for history | Disposable account rows were created/cleaned by existing fixture inside disposable DB; all temporary services/processes stopped. Existing adjacent-route TanStack not-found warnings appeared during contract-boundary tests; no suite failure. Existing pnpm scope and `NO_COLOR`/`FORCE_COLOR` warnings are non-blocking. | CA-01–CA-03, CA-05–CA-08, CA-10–CA-12; CI-05 |
| EV-17 | 2026-09-23 | Live authenticated persisted Activity → result browser flow (historical for result details; retain flow evidence) | Sign in against isolated seeded full-stack Testcontainers; GET Activity → 200; submit new attempt → 201; verify attempt persisted; registered Inngest evaluation completed/callback 200; GET attempt result → 200; inspect final route, DOM, requests and console; capture `VM-01-live-desktop-question.png`, `VM-02-live-mobile-unsent.png`, `VM-03-live-desktop-completed.png`, `VM-04-live-mobile-completed.png` | Attempt creation, persistence, job completion and result retrieval passed. Result screenshot exposed missing per-question cards because Web adapter wire names did not match Server JSON; result-detail, disclosure and visual claims are stale pending ACH-06 correction. | Disposable PostgreSQL/Redis/Inngest and API/Web processes stopped; shared Compose services and data untouched. No failed browser requests or console errors in the successful run. Artifacts are under `/tmp/shifu-activity-choice-evidence/`. | CA-01–CA-06, CA-10, CA-12; VM-01–VM-04; VIS-07, VIS-09 |
| EV-18 | 2026-09-23 | ACH-06 scoped Web correction | `/root/learning_activity_web_builder` changed only `apps/web/src/rest/services/learning-service.ts`; `pnpm --filter web check:types` → exit 0 | Wire adapter now maps server `question_key`, `selected_option_keys`, `disclosed_correct_option_keys` to Page fields. Typecheck passed; this did not by itself resolve the live missing-detail observation, now tracked as ACH-07. | The initial unauthenticated mock visual capture and the first real-flow attempt were preserved as failed evidence; the former retained the login page and emitted fixture query errors, the latter reached the pending state before the job was ready. Both were superseded by EV-20's fresh capture run. | Only Web adapter path changed by Builder Web; no product contract change | CA-06–CA-08 |
| EV-19 | 2026-09-23 | Authenticated persisted flow after ACH-06 correction; result detail diagnosis | Isolated full-stack Playwright scenario with disposable PostgreSQL/Redis/Inngest: login; Activity GET; unsent mobile navigation warning; submit → one persisted attempt; wait for completed result; database showed `answer_count=3`, `part_count=3`; assert Result Page details | Attempt persistence and evaluation completion passed. Fresh assertion `section[aria-label="Detalhes por questão"] > *` expected 3, received 0. Result page still showed score/progress/recommendation; CA-06–CA-08 and VM-03/VM-04 remain blocked. | Successful HTTP response statuses only; no browser console errors. Exact VM images remain in `/tmp/shifu-activity-choice-evidence/`. All app/Testcontainers stopped; shared Compose untouched. Web Builder's adapter correction is present; Server controller response verification is active as ACH-07. | CA-01–CA-06, CA-10, CA-12; VM-01–VM-04; ACH-07 |
| EV-20 | 2026-09-23 | Authenticated visual state capture, supplement to live flow | With real seeded account session and isolated full-stack services, capture VIS-01–VIS-12 at their Spec viewports using post-login Page responses for designed safe/pending/failure/recovery states; test passed; inspect current screenshots | Twelve fresh clean screenshots captured; Activity questions, result layouts, protected/released details, pending, failed and recovered states are visible without the login page. These mock-backed captures establish visual composition/state only; they do not close ACH-07 or replace the persisted-flow result assertion. | Files `VIS-01-desktop-single.png` through `VIS-12-desktop-recovered.png` under `/tmp/shifu-activity-choice-evidence/`; Browser console/request checks on the live transport flow were clear. | Disposable services stopped; no shared data touched. References still require Orchestrator comparison and reviewer replay. | CA-01–CA-10; VM-02–VM-04; VIS-01–VIS-12 |
| EV-21 | 2026-09-23 | ACH-07 live persisted-flow confirmation and current VM captures | `pnpm --filter web exec biome check --write tests/learning/live-choice-flow.temp.spec.ts`; `cd apps/server && uv run python /tmp/shifu_run_live_activity.py` → 1/1 Playwright scenario passed; seeded login, read Activity, mobile unsent warning, submit, verify one new attempt, wait for completed evaluation, inspect DB, assert 3 Result Page detail cards, capture VM-01–VM-04 and VIS-01–VIS-12; inspect screenshots against nine saved references | Completed attempt persisted with 3 answers and 3 typed evaluation parts; the registered job completed; live Result Page rendered exactly 3 question detail cards. No failed browser requests or console errors. VIS layouts/states match the approved hierarchy and disclosure behavior. VM-03/VM-04 and result visuals show `100.00`, logged as ACH-08; affected result visuals are not final until recaptured after its correction. | Isolated PostgreSQL 17, Redis 7, Inngest v1.41.1 plus API/Web processes stopped by runner; shared Compose data untouched. Captures are `/tmp/shifu-activity-choice-evidence/VM-01-live-desktop-question.png` through `VM-04-live-mobile-completed.png` and `VIS-01-desktop-single.png` through `VIS-12-desktop-recovered.png`. VM-03 is 1440×900 and VM-04 is 390×844. | CA-01–CA-08, CA-10, CA-12; VM-01–VM-04; VIS-01–VIS-12; ACH-07/08 |
| EV-22 | 2026-09-23 | ACH-08 scoped Web display correction | Builder Web changed only both Choice Result score renderers and their colocated suites; `pnpm --filter web exec vitest run src/ui/learning/widgets/pages/choice-result-page/tests/choice-result-page.test.tsx src/ui/learning/widgets/pages/choice-result-page/choice-result-detail/tests/choice-result-detail.test.tsx` → 2 files/10 tests passed; scoped Biome on four paths → clean; `pnpm --filter web check:types` → exit 0 | Decimal-string values such as `100.00` render as `100`; `72.50` renders as `72,5` at page and detail boundaries. Domain/API scoring remains untouched. Builder report inspected; Orchestrator integrated-candidate reruns and fresh result captures remain pending. | Existing `pnpm.onlyBuiltDependencies` warning only. Browser and broad Web gates not yet rerun after this visual correction. | CA-04, CA-06; ACH-08 |
| EV-23 | 2026-09-23 | First temporary Playwright recapture attempt after ACH-08 | `pnpm --filter web exec biome check --write tests/learning/live-choice-flow.temp.spec.ts`; `cd apps/server && uv run python /tmp/shifu_run_live_activity.py` → test stopped at sign-in assertion after the browser submitted `/login?email=…&password=…` | Failed before entering the feature flow or capturing fresh evidence. Indicates the temporary harness submitted before login-page hydration; does not affect application gates or prior EV-21 server-backed evidence. | Testcontainers/API/Web runner cleaned up; shared Compose untouched. Exact Playwright error is in ignored `apps/web/test-results/learning-live-choice-flow.../error-context.md`; preserve this attempt and retry after waiting for login hydration. | ACH-08; VM-01–VM-04; VIS-01–VIS-12 |
| EV-24 | 2026-09-23 | Current authenticated persisted flow and refreshed VM/VIS screenshots after ACH-08 | `cd apps/server && uv run python /tmp/shifu_run_live_activity.py` → 1/1 Playwright scenario passed; actual seeded login, safe Activity read, mobile unsent leave guard, sequential answer submission, one new attempt, persisted 3 answers/3 result parts, completed result with exactly 3 details; capture VM-01–VM-04 and VIS-01–VIS-12 | Current live result renders compact score (`100 / 100`) and all persisted details. No failed requests or console errors. All screenshots inspected against the nine saved references; protected/correct, pending, failure and recovered states match their intended hierarchy and disclosure rules. | Disposable PostgreSQL 17/Redis 7/Inngest v1.41.1 plus API/Web processes stopped; shared Compose/data untouched. Artifacts `/tmp/shifu-activity-choice-evidence/VM-01-live-desktop-question.png` through `VM-04-live-mobile-completed.png`, and `VIS-01-desktop-single.png` through `VIS-12-desktop-recovered.png`. | CA-01–CA-08, CA-10, CA-12; VM-01–VM-04; VIS-01–VIS-12; ACH-07/08 |
| EV-25 | 2026-09-23 | First full-suite wrapper attempt after ACH-08 | `/tmp/shifu_run_web_integration.py` initially invoked `pnpm --filter web exec playwright test test:integration --workers=1`; Playwright reported “No tests found” | Harness command was composed incorrectly; no application assertion ran. Corrected wrapper to invoke the declared `pnpm --filter web test:integration --workers=1` script and reran as EV-26. | Disposable services and API cleaned up by wrapper; shared Compose untouched. | CI-05 |
| EV-26 | 2026-09-23 | Full Web Playwright suite on current candidate | With disposable PostgreSQL 17/Redis 7/Inngest v1.41.1, API at 7777 and `BETTER_AUTH_DATABASE_URL` bound to that database: `pnpm --filter web test:integration --workers=1` → 51/51 passed, including all current route/Page suites and EV-24 live-capture scenario | Passed. Browser suite showed the pre-existing adjacent-route TanStack not-found warnings; a Home layout fixture logged `Query data cannot be undefined` for `learning/home-goals`, while its test and suite passed. Choice Activity live scenario reported no failed requests or console errors. No Learning flow warning was observed. | Testcontainers/API/Web stopped by wrapper. Existing `pnpm.onlyBuiltDependencies`, `NO_COLOR`/`FORCE_COLOR` and adjacent route warnings only; no shared service or data touched. | CI-05; CA-01–CA-03, CA-05–CA-08, CA-10–CA-12 |
| EV-27 | 2026-09-23 | Initial five-question mobile screenshot harness (invalid visual artifact) | `uv run python /tmp/shifu_run_five_question.py` → one test passed five-card/overflow assertions, but `authenticatedPage` fixture left the login screen in the capture above the routed result | Screenshot was invalid as a visual artifact because it included the sign-in shell; keep it historical and use only the corrected real-login capture in EV-28. | The invalid screenshot at `/tmp/shifu-activity-choice-evidence/VM-04-live-five-question-mobile.png` was overwritten by EV-28; no source code change. | CA-06, CA-10; VM-04 |
| EV-28 | 2026-09-23 | Authenticated five-question mobile result and navigation obstruction check | `uv run python /tmp/shifu_run_five_question.py` → 1/1 Playwright scenario passed using real seeded login then post-login result responses; assert five details, `scrollWidth <= clientWidth`, scroll last card, assert its bottom is above `Navegação inferior`, capture at 390 × 844 | Passed: all five question details render; no horizontal overflow; final card is fully scrollable above the fixed bottom navigation. Fresh screenshot inspected and contains only the authenticated shell/result. Response data is mock-backed, so it supports five-question composition/scroll evidence; EV-24 supplies real persisted three-question evidence. | Disposable services/API/Web stopped; screenshot `/tmp/shifu-activity-choice-evidence/VM-04-live-five-question-mobile.png`. | CA-06, CA-10; VM-04 |
| EV-29 | 2026-09-23 | Final Server gates after ACH-07 mapper fix | `uv run poe check:lint` → 534 files formatted and lint clean; `check:architecture` → all modules validated; `check:types` → 0 diagnostics; `test:unit` → 42 passed; `test:integration` → 33 passed; `test:jobs` → 2 passed; `build` → exit 0; `git diff --check` → exit 0 | Passed on the integrated Server candidate. The real controller regression returns all three results and hides correct choices for incorrect answers. | Integration and job suites used disposable PostgreSQL/Redis/Inngest fixtures; shared Compose untouched. Existing Starlette/httpx and status-code deprecations and package build's missing-README warning only. | CI-07–CI-13; CA-01, CA-03–CA-09, CA-11–CA-12; VM-05–VM-07 |
| EV-30 | 2026-09-23 | Final Web gates after ACH-08 display correction and generated-route reconciliation | `pnpm --filter web check:lint` → 162 files clean; `check:architecture` → 156 modules/468 dependencies; `check:types` → exit 0; `test:unit` → 29 files/86 tests passed; `pnpm --filter web build` → exit 0; `git diff --check` → exit 0. CI-05 full Playwright suite is EV-26. | Passed. Generated `routeTree.gen.ts` maps all three Activity/attempt routes; the corrected Page renders compact values without changing API/domain scoring. | Existing pnpm `onlyBuiltDependencies` warning; Web test output's unrelated Query undefined fixture error and adjacent-route not-found warnings are classified under EV-26. No temporary Playwright harness remains in the repository. | CI-01–CI-06; CA-01–CA-12; REST parity; ACH-08 |
| EV-31 | 2026-09-23 | Initial read-only Implementation Reviewer pass | Reviewer inspected the current Spec rev3, Plan, integrated diff, Rule Pack, Evaluation evidence and visual artifacts; reported one actionable code finding (ACH-09) and stale F2-T1/F2-T2 Plan task-card statuses | Initial pass found direct `useNavigate` calls and hardcoded `/login` in both Activity route wrappers, contrary to `ui-layer-rules.md`. Orchestrator corrected F2-T1 status and reopened F2-T2 for the route correction. Resume this same reviewer after ACH-09 and reruns. | This review is advisory until the finding is corrected and independently rechecked; no visual output changed. | ACH-09; CI-01–CI-06; route suites |
| EV-32 | 2026-09-23 | ACH-09 integrated Web gates and route regression | Builder: `pnpm --filter web exec playwright test 'tests/routes/learning/activities.*index.test.tsx'` → 6/6; scoped Biome (three changed files) and `pnpm --filter web check:types` passed. Orchestrator: `pnpm --filter web check:lint` → 162 files clean; `check:architecture` → 156 modules/468 dependencies; `check:types` passed; `test:unit` → 29 files/86 tests; `pnpm --filter web build` passed; `uv run python /tmp/shifu_run_web_integration.py` from `apps/server` → full isolated Playwright suite 50/50; root `git diff --check` passed. A first `python` invocation failed because that executable alias is absent; running `uv` from repository root failed because root has no `pyproject.toml`; rerun from `apps/server` succeeded. | Passed. Existing route suites verify submission-to-attempt, return-to-Activity and unresolved-attempt navigation. No feature route directly calls `useNavigate` or hardcodes `/login`. Full browser suite emitted existing adjacent-route TanStack not-found warnings and the unrelated Home layout fixture's undefined `learning/home-goals` query warning; all 50 tests passed. | The integration wrapper used disposable PostgreSQL 17/Redis 7/Inngest v1.41.1 and stopped its app processes/containers; shared Compose services/data untouched. Builder route run logged one aborted HTTP 500 during teardown after its six passing tests. UI rendered output was unchanged; existing VM/VIS captures remain current. Existing pnpm app-scope `onlyBuiltDependencies` warning. | ACH-09; CI-01–CI-06; RF-01–RF-03, RF-05–RF-08; CA-01–CA-03, CA-05–CA-08, CA-10, CA-12 |
| EV-33 | 2026-09-23 | Resumed Implementation Reviewer pass | The same read-only reviewer verified the corrected Activity/attempt wrappers against `ui-layer-rules.md`, confirmed F2-T1/F2-T2 task cards match the Plan wave table, and inspected the corrected candidate and EV-32 | Reviewer confirmed ACH-09 and the stale task-card finding are resolved; no remaining findings. Candidate is ready for local conclusion. | Same Implementation Reviewer as EV-31; no source or evidence changes requested. | ACH-09; Plan F2 task status; all RF/CA/VM/CI |

## Final implementation conformance

- **Contract:** `documentation/features/learning/activity-choice-questions/spec.md`,
  revision 3, status `completed`; source SHIFU-74. Learning PRD content `83066881`
  version 13 and Curriculum PRD content `83034113` version 6 were reread in full on
  2026-09-23 and still match the selected contract. Jira remains `A fazer`.
- **Scope/diff:** the integrated delivery matches Plan F1/F2 and scoped ACH-06–ACH-09
  assignments. It adds Curriculum choice snapshots, Learning attempt/evaluation domain
  and persistence, migration, controllers, four REST operations, outbox/Inngest job,
  Web service/widgets/routes, generated route registration, route/Page/unit/integration
  tests and the four-request `apps/server/rest-client/learning/activities.rest` file.
  `apps/web/src/routeTree.gen.ts` is generated and was reconciled with
  `pnpm --filter web generate-routes` (EV-14/EV-30); no route metadata was edited by hand.
- **Contract coverage:** all required paths, safe question/result DTOs, ownership and
  disclosure rules, immutable/idempotent submissions, pending/failure/recovery, exact
  scoring, once-only progress effects, responsive states, keyboard/focus, and protected
  answer behavior are covered by CA-01–CA-12, CI-01–CI-15, VM-01–VM-07 and VIS-01–VIS-12.
  Real persisted Activity → Inngest evaluation → result evidence is EV-24; migration
  lifecycle is EV-13; REST route parity is EV-14; five-question mobile scroll is EV-28.
- **Review:** the single read-only Implementation Reviewer completed its initial pass
  (EV-31) and resumed pass (EV-33). The Plan statuses now match their phase table; all
  findings ACH-01–ACH-09 are resolved. The Reviewer confirmed the two Activity route
  wrappers use the shared navigation hook and canonical login route, with no remaining
  findings.
- **Exclusions:** no diagnosis/code Activity, history UI, unsent-answer persistence,
  automatic evaluation retries, extra dependencies, or external Jira/Confluence writes
  were introduced. Pre-existing user-owned `design/shifu.pen` changes are preserved and
  excluded from this delivery's edits/staging. No credential or temporary browser artifact
  is included in tracked paths.
- **Limitations:** `pnpm` reports the existing app-scope `onlyBuiltDependencies`
  configuration warning. The full browser suite also reports adjacent-route TanStack
  not-found warnings and an unrelated Home fixture's undefined `learning/home-goals`
  query; all 50 tests pass. No committed `.github/workflows` directory exists, so PR CI
  is not applicable. Disposable services and API/Web processes were stopped; shared
  Compose services/data remain untouched.

## Delivery disposition

| PRD/Journey | Selected scope | Evidence | Local disposition | PRD checkbox |
| --- | --- | --- | --- | --- |
| Learning RP-09, RP-10, RP-13–RP-18, RP-25, RP-26 | Choice Activity attempt, evaluation, safe result and responsive experience slice | CA-01–CA-12; EV-13–EV-14, EV-24, EV-26, EV-28–EV-32; VM-01–VM-07; VIS-01–VIS-12 | `partially_implemented` — this delivery implements the selected choice-question slice of broader Learning requirements | Unchanged |
| Learning JN-07 | Answer and receive the official result for a learning Activity | CA-01–CA-12; EV-24, EV-26, EV-32 | `partially_implemented` — the supported choice Activity path is delivered; adjacent Activity formats remain separate | Unchanged |
| Curriculum RP-03, RP-04 | Authored single-choice/multiple-selection content and fixed outcome explanation snapshot consumed by Learning | CA-01, CA-04, CA-07, CA-08, CA-11; EV-13–EV-15, EV-24, EV-29 | `partially_implemented` — Learning consumes this bounded authored-content contract; Curriculum authoring/admin scope is excluded | Unchanged |

## Lessons and authority disposition

| Finding/lesson | Authority review | Disposition |
| --- | --- | --- |
| ACH-01 identified that routed Page tests and per-route tests are distinct required boundaries. | Spec revision 3 now names both module-owned Page suites and all three route suites; no global Rule update needed. | No change to Rules; the selected Rules already state the test-boundary requirements. |
| ACH-09 found route wrappers bypassed the shared imperative-navigation hook. | The accepted `ui-layer-rules.md` already requires the shared hook and canonical paths; shared hook overload preserves existing typed consumers. | No change to Rules or Architecture; feature-local correction fully addresses the finding. |
| Temporary harness command/login attempts and the adjacent TanStack/Home fixture warnings. | No product or implementation contract defect; final exact commands and warnings are retained in EV-23, EV-25, EV-27 and EV-32. | No durable documentation change for transient harness/environment details or unrelated existing warnings. |

**Publication:** local SDD artifacts are complete. No commit, push, PR, merge, deployment,
Jira transition, or Confluence edit was performed in this task.
